[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "konkan-ai"
version = "0.1.0"
description = "Konkan card game prototype"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
